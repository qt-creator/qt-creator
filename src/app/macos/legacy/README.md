This is a check-in of the .icns file that should be generated from the .icon
file.
Generating assets from the .icon file with `actool` is only supported since
Xcode 26, so we are using this instead for earlier versions.
