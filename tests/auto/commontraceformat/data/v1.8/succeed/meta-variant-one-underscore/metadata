/* CTF 1.8 */

trace {
	major = 1;
	minor = 8;
	byte_order = be;
};

event {
	name = yo;
	fields := struct {
		enum : integer { size = 8; } {
			COSSETTE,
			PELCHAT,
			_VOISINE,
		} tag;

		variant <tag> {
			string COSSETTE;
			string PELCHAT;
			string _VOISINE;
		} var;
	};
};
