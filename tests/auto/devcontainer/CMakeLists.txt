SET(ENABLE_DEVCONTAINER_TST TRUE)
if (CMAKE_CXX_COMPILER_ID STREQUAL "MSVC" AND CMAKE_CXX_COMPILER_VERSION VERSION_LESS 19.30)
    SET(ENABLE_DEVCONTAINER_TST FALSE)
endif()

add_qtc_test(tst_devcontainer
  DEPENDS Tasking DevContainer
  CONDITION ENABLE_DEVCONTAINER_TST
  SOURCES tst_devcontainer.cpp
)
