@if "%{QtCompatibility}" == "Qt5+"
import QtQuick 2.15
import QtTest 1.0
@endif
@if "%{QtCompatibility}" == "Qt6+"
import QtQuick
import QtTest
@endif

TestCase {
    name: "%{TestCaseName}"

@if "%{GenerateInitAndCleanup}" == "true"
    function initTestCase() {
    }

    function cleanupTestCase() {
    }

@endif
    function test_case1() {
        compare(1 + 1, 2, "sanity check");
        verify(true);
    }
}
